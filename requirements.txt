# KrishiMind SustainAI - Production Dependencies
# Pinned versions for reproducibility
# Updated for Python 3.12+ compatibility

# Data Processing (latest versions with Python 3.12+ support)
pandas>=2.2.0,<3.0.0
numpy>=1.26.0,<2.0.0

# Machine Learning
# CRITICAL: scikit-learn pinned to 1.6.1 (exact version models were trained with)
# Using a newer version (e.g., 1.8.0) may cause prediction inconsistencies
# Models must be retrained if this version is updated
scikit-learn==1.6.1
xgboost>=2.0.0,<3.0.0
lightgbm>=4.0.0,<5.0.0
joblib>=1.3.0,<2.0.0

# Deep Learning (Plant Disease Detection)
torch>=2.3.0
torchvision>=0.18.0
timm>=1.0.0
Pillow>=10.0.0

# API Framework
fastapi>=0.109.0,<1.0.0
uvicorn>=0.27.0,<1.0.0
pydantic>=2.0.0,<3.0.0

# AWS Lambda Adapter
mangum==0.17.0

# HTTP Client
requests>=2.31.0,<3.0.0

# Testing Framework
pytest>=7.4.0,<8.0.0
pytest-asyncio>=0.23.0,<1.0.0
